# Computing the within-cluster sum of squares for the linear kernels

Computes the within-cluster sum of squares for the linear kernel
associated with X

## Usage

``` r
WithinClusterSS_linear(X, H)
```
