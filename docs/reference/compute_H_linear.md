# Compute top eigenvectors (H) of the combined kernel using linear kernels

Compute top eigenvectors (H) of the combined kernel using linear kernels

## Usage

``` r
compute_H_linear(X_m_list, theta, centers)
```
