# Renaming CTN cluster labels

Internal function for renaming CTN cluster labels based on the
proportion of driver cell type triads

## Usage

``` r
label_CTN(CTN_table, driver_celltypes = NULL, clusternum = 2)
```
