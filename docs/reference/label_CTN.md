# Renaming CTN cluster labels

Internal function for renaming CTN cluster labels based on the
proportion of core cell type triads

## Usage

``` r
label_CTN(CTN_table, core_celltypes = NULL, clusternum = 2)
```
